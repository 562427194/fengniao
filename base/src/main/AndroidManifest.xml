<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="mybase.library">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        >
        <activity
            android:name="mybase.libaray.activity.selectimg.MultiImageSelectorActivity"
            />
        <activity
            android:name="mybase.library.activity.WebViewActivity"
            />
        <activity android:name="mybase.library.activity.ShowImageActivity"/>

    </application>

</manifest>
